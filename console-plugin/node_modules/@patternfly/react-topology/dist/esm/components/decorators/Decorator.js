import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { css } from '@patternfly/react-styles';
import styles from '../../css/topology-components';
import SvgDropShadowFilter from '../svg/SvgDropShadowFilter';
import { createSvgIdUrl, useHover } from '../../utils';
import { DEFAULT_DECORATOR_PADDING } from '../nodes';
import useCombineRefs from '../../utils/useCombineRefs';
const HOVER_FILTER_ID = 'DecoratorDropShadowHoverFilterId';
const Decorator = ({ className, x, y, showBackground, radius, padding = DEFAULT_DECORATOR_PADDING, children, icon, onClick, ariaLabel, circleRef, innerRef }) => {
    const [hover, hoverRef] = useHover();
    const iconRadius = radius - padding;
    return (_jsxs("g", Object.assign({ ref: useCombineRefs(hoverRef, innerRef), className: css(styles.topologyNodeDecorator, className) }, (onClick
        ? {
            onClick: (e) => {
                e.stopPropagation();
                onClick(e);
            },
            role: 'button',
            'aria-label': ariaLabel
        }
        : null), { children: [_jsx(SvgDropShadowFilter, { id: HOVER_FILTER_ID, dy: 3, stdDeviation: 5, floodOpacity: 0.5 }), showBackground && (_jsx("circle", { ref: circleRef, className: css(styles.topologyNodeDecoratorBg), cx: x, cy: y, r: radius, filter: hover ? createSvgIdUrl(HOVER_FILTER_ID) : undefined }, hover ? 'circle-hover' : 'circle')), _jsxs("g", Object.assign({ transform: `translate(${x}, ${y})` }, { children: [icon ? (_jsx("g", Object.assign({ className: css(styles.topologyNodeDecoratorIcon), style: { fontSize: `${iconRadius * 2}px` }, transform: `translate(-${iconRadius}, -${iconRadius})` }, { children: icon }))) : null, children] }))] })));
};
export default Decorator;
//# sourceMappingURL=Decorator.js.map