import { __rest } from "tslib";
import { jsx as _jsx } from "react/jsx-runtime";
import { observer } from 'mobx-react';
import DefaultGroupExpanded from './DefaultGroupExpanded';
import { isNode } from '../../types';
import { Dimensions } from '../../geom';
import DefaultGroupCollapsed from './DefaultGroupCollapsed';
const DefaultGroupInner = observer((_a) => {
    var { className, element, onCollapseChange } = _a, rest = __rest(_a, ["className", "element", "onCollapseChange"]);
    const handleCollapse = (group, collapsed) => {
        if (collapsed && rest.collapsedWidth !== undefined && rest.collapsedHeight !== undefined) {
            group.setDimensions(new Dimensions(rest.collapsedWidth, rest.collapsedHeight));
        }
        group.setCollapsed(collapsed);
        onCollapseChange && onCollapseChange(group, collapsed);
    };
    if (element.isCollapsed()) {
        return (_jsx(DefaultGroupCollapsed, Object.assign({ className: className, element: element, onCollapseChange: handleCollapse }, rest)));
    }
    return _jsx(DefaultGroupExpanded, Object.assign({ className: className, element: element, onCollapseChange: handleCollapse }, rest));
});
const DefaultGroup = (_a) => {
    var { element } = _a, rest = __rest(_a, ["element"]);
    if (!isNode(element)) {
        throw new Error('DefaultGroup must be used only on Node elements');
    }
    return _jsx(DefaultGroupInner, Object.assign({ element: element }, rest));
};
export default DefaultGroup;
//# sourceMappingURL=DefaultGroup.js.map