import { jsx as _jsx } from "react/jsx-runtime";
import { Fragment, PureComponent } from 'react';
export class Defs extends PureComponent {
    constructor(props) {
        super(props);
        this.state = {};
    }
    setDefs(defs) {
        // setting the state will re-render this component
        this.setState({ defs: Object.assign({}, defs) });
    }
    render() {
        const { defs } = this.state;
        return defs ? (_jsx("defs", { children: Object.keys(defs).map((id) => (_jsx(Fragment, { children: defs[id].node }, id))) })) : null;
    }
}
//# sourceMappingURL=Defs.js.map