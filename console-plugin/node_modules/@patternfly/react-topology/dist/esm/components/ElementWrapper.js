import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { useMemo } from 'react';
import { observer } from 'mobx-react';
import { computed } from 'mobx';
import ElementContext from '../utils/ElementContext';
import { isGraph, isEdge, isNode } from '../types';
import { ATTR_DATA_ID, ATTR_DATA_KIND, ATTR_DATA_TYPE } from '../const';
import ComputeElementDimensions from './ComputeElementDimensions';
import { useDndManager } from '../behavior/useDndManager';
const NodeElementComponent = observer(({ element }) => {
    const dndManager = useDndManager();
    const isDragging = dndManager.isDragging();
    const dragItem = dndManager.getItem();
    const controller = element.hasController() && element.getController();
    const isVisible = useMemo(() => computed(() => controller && controller.shouldRenderNode(element)), [element, controller]);
    if (isVisible.get() || (isDragging && dragItem === element)) {
        return _jsx(ElementComponent, { element: element });
    }
    return null;
});
// in a separate component so that changes to behaviors do not re-render children
const ElementComponent = observer(({ element }) => {
    const kind = element.getKind();
    const type = element.getType();
    const controller = element.hasController() && element.getController();
    const Component = useMemo(() => controller && controller.getComponent(kind, type), [controller, kind, type]);
    if (Component) {
        return (_jsx(ElementContext.Provider, Object.assign({ value: element }, { children: _jsx(Component, Object.assign({}, element.getState(), { element: element })) })));
    }
    return null;
});
const ElementChildren = observer(({ element, level }) => (_jsxs(_Fragment, { children: [element
            .getChildren()
            .filter(isEdge)
            .map((e) => (_jsx(ElementWrapper, { element: e }, e.getId()))), element
            .getChildren()
            .filter(isNode)
            .map((e) => (_jsx(ElementWrapper, { element: e, level: level }, e.getId())))] })));
const ElementWrapper = observer(({ element, level = 0 }) => {
    if (!element.isVisible()) {
        if (!isNode(element) || element.isDimensionsInitialized()) {
            return null;
        }
    }
    if (isEdge(element)) {
        const source = element.getSourceAnchorNode();
        const target = element.getTargetAnchorNode();
        if ((source && !source.isVisible()) || (target && !target.isVisible())) {
            return null;
        }
    }
    const commonAttrs = {
        [ATTR_DATA_ID]: element.getId(),
        [ATTR_DATA_KIND]: element.getKind(),
        [ATTR_DATA_TYPE]: element.getType()
    };
    if (isGraph(element)) {
        return (_jsx("g", Object.assign({}, commonAttrs, { children: _jsx(ElementComponent, { element: element }) })));
    }
    if (isNode(element)) {
        if (!element.isDimensionsInitialized()) {
            return (_jsxs(ComputeElementDimensions, Object.assign({ element: element }, { children: [_jsx(ElementComponent, { element: element }), _jsx(ElementChildren, { element: element })] })));
        }
        if (!element.isGroup() || element.isCollapsed()) {
            const { x, y } = element.getPosition();
            return (_jsxs("g", Object.assign({}, commonAttrs, { transform: `translate(${x}, ${y})` }, { children: [_jsx(NodeElementComponent, { element: element }), _jsx(ElementChildren, { element: element })] })));
        }
        return (_jsxs("g", Object.assign({}, commonAttrs, { children: [_jsx(NodeElementComponent, { element: element }), _jsx(ElementChildren, { element: element })] })));
    }
    return (_jsxs("g", Object.assign({}, commonAttrs, { style: { zIndex: level } }, { children: [_jsx(ElementComponent, { element: element }), _jsx(ElementChildren, { element: element, level: level + 1 })] })));
});
export default ElementWrapper;
//# sourceMappingURL=ElementWrapper.js.map