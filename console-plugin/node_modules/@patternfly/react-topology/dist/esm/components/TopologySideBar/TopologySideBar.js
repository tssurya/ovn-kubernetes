import { __rest } from "tslib";
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useState, useEffect, Fragment } from 'react';
import { css } from '@patternfly/react-styles';
import styles from '../../css/topology-side-bar';
import TimesIcon from '@patternfly/react-icons/dist/esm/icons/times-icon';
import { Button } from '@patternfly/react-core';
export const TopologySideBar = (_a) => {
    var { className = '', resizable = false, show, onClose = null, header, children = null } = _a, otherProps = __rest(_a, ["className", "resizable", "show", "onClose", "header", "children"]);
    const [isIn, setIsIn] = useState(false);
    useEffect(() => {
        let timer = null;
        if (isIn !== show) {
            clearTimeout(timer);
            timer = setTimeout(() => setIsIn(show), 150);
        }
        return () => {
            clearTimeout(timer);
        };
    }, [show, isIn]);
    const classNames = resizable
        ? css(styles.topologyResizableSideBar, className)
        : css(styles.topologySideBar, 'fade', className, show && styles.shown, isIn && styles.in);
    return (_jsx("div", Object.assign({}, otherProps, { role: "dialog", className: classNames }, { children: (resizable || show) && (_jsxs(Fragment, { children: [onClose && (_jsx(Button, { icon: _jsx(TimesIcon, {}), className: css(styles.topologySideBarDismiss), variant: "plain", onClick: onClose, "aria-label": "Close" })), header && _jsx("div", Object.assign({ className: styles.topologySideBarHeader }, { children: header })), children] })) })));
};
TopologySideBar.displayName = 'TopologySideBar';
//# sourceMappingURL=TopologySideBar.js.map