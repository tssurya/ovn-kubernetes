import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useRef } from 'react';
import { Tooltip, TooltipPosition } from '@patternfly/react-core';
import TrashIcon from '@patternfly/react-icons/dist/esm/icons/trash-icon';
/**
 * @param startPoint
 * @param endPoint
 */
function computeTooltipPosition(startPoint, endPoint) {
    const angle = Math.abs((Math.atan2(endPoint.y - startPoint.y, endPoint.x - startPoint.x) * 180) / Math.PI);
    if (angle < 135 && angle > 90) {
        return TooltipPosition.left;
    }
    if (angle > 45 && angle <= 90) {
        return TooltipPosition.right;
    }
    return TooltipPosition.top;
}
const DefaultRemoveConnector = ({ startPoint, endPoint, onRemove, size = 14 }) => {
    const DefaultRemoveConnectorRef = useRef(null);
    return (_jsx(Tooltip, Object.assign({ triggerRef: DefaultRemoveConnectorRef, content: "Delete Connector", position: computeTooltipPosition(startPoint, endPoint) }, { children: _jsxs("g", Object.assign({ transform: `translate(${startPoint.x + (endPoint.x - startPoint.x) * 0.5}, ${startPoint.y + (endPoint.y - startPoint.y) * 0.5})`, onClick: (e) => {
                e.stopPropagation();
                onRemove();
            }, ref: DefaultRemoveConnectorRef }, { children: [_jsx("circle", { className: "topology-connector__remove-bg", cx: 0, cy: 0, r: size }), _jsx("g", Object.assign({ transform: `translate(-${size / 2}, -${size / 2})` }, { children: _jsx(TrashIcon, { className: "topology-connector__remove-icon", style: { fontSize: size } }) }))] })) })));
};
export default DefaultRemoveConnector;
//# sourceMappingURL=DefaultRemoveConnector.js.map