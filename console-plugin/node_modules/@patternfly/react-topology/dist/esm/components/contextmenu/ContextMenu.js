import { __rest } from "tslib";
import { Fragment as _Fragment, jsx as _jsx } from "react/jsx-runtime";
import { useState, useCallback, useEffect } from 'react';
import { Dropdown } from '@patternfly/react-core';
import { css } from '@patternfly/react-styles';
import topologyStyles from '../../css/topology-components';
// FIXME fully qualified due to the effect of long build times on storybook
import Popper from '../popper/Popper';
const ContextMenu = (_a) => {
    var { children, open = true, onRequestClose } = _a, other = __rest(_a, ["children", "open", "onRequestClose"]);
    const [isOpen, setOpen] = useState(!!open);
    useEffect(() => {
        setOpen(open);
    }, [open]);
    const handleOnRequestClose = useCallback(() => {
        onRequestClose ? onRequestClose() : setOpen(false);
    }, [onRequestClose]);
    useEffect(() => {
        if (isOpen) {
            setOpen(false);
            requestAnimationFrame(() => setOpen(true));
        }
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [children]);
    return (_jsx(Popper, Object.assign({}, other, { closeOnEsc: true, closeOnOutsideClick: true, open: isOpen, onRequestClose: handleOnRequestClose }, { children: _jsx(Dropdown, Object.assign({ onSelect: handleOnRequestClose, toggle: () => _jsx(_Fragment, {}), className: css(topologyStyles.topologyContextMenuCDropdownMenu), popperProps: { appendTo: 'inline' } }, { children: children })) })));
};
export default ContextMenu;
//# sourceMappingURL=ContextMenu.js.map