import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { forwardRef } from 'react';
import { css } from '@patternfly/react-styles';
import styles from '../../../css/topology-components';
const LabelIcon = forwardRef(({ className, x, y, width, height, iconClass, icon, padding = 4, innerRef }, circleRef) => {
    const radius = width / 2;
    const cx = x - radius;
    const cy = y + height / 2;
    const innerX = x - width + padding + 1;
    const innerY = y + padding + 1;
    const innerWidth = width - padding * 2 - 2; // -2 for 1px border on each side
    const innerHeight = height - padding * 2 - 2; // -2 for 1px border on each side
    return (_jsxs("g", Object.assign({ className: css(styles.topologyNodeLabelIcon, className), ref: innerRef }, { children: [_jsx("circle", { className: css(styles.topologyNodeLabelIconBackground), ref: circleRef, cx: cx, cy: cy, r: radius }), icon ? (_jsx("foreignObject", Object.assign({ className: css(styles.topologyNodeLabelIcon), x: innerX, y: innerY, width: Math.max(innerWidth, 0), height: Math.max(innerHeight, 0) }, { children: icon }))) : (_jsx("image", { x: innerX, y: innerY, width: Math.max(innerWidth, 0), height: Math.max(innerHeight, 0), xlinkHref: iconClass }))] })));
});
export default LabelIcon;
//# sourceMappingURL=LabelIcon.js.map